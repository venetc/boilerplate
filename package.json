{
  "name": "Test",
  "version": "0.0.1",
  "description": "",
  "license": "",
  "author": "",
  "scripts": {
    "clean:output": "rimraf dist",
    "build": "npm run clean:output && parcel build index.html",
    "dev": "npm run clean:output && parcel index.html --open",
    "start": "npm run build && npm run dev"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "rimraf": "^3.0.2",
    "sass": "^1.49.9"
  },
  "dependencies": {}
}
